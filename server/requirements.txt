cryptography>=42.0.0

# Опционально: ускоряет asyncio в 2-4 раза. На слабом VPS заметно.
uvloop>=0.19.0; sys_platform != "win32"

# Нужен только при KEYSTORE_BACKEND=redis (несколько серверов).
# Для одного сервера хватает SQLite — устанавливать не обязательно.
redis>=5.0.0

# Только для разработки:
pytest>=8.0.0
pytest-asyncio>=0.23.0
fakeredis>=2.20.0
